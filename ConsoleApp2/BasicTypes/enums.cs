using System;
using System.Collections.Generic;
using System.Text;

namespace SmartCoin
{
    public enum BitActionType { Buy, Sell, Nothing }
    public enum SiteName { BitFinex, Binance }
    public enum CoinName { Ripple, Bitcoin,USD }
}
